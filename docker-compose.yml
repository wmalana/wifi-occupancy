services:
  wifi-occupancy:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data       # SQLite database (persisted)
      - ./config:/app/config   # sites.yaml
    env_file: .env
    restart: unless-stopped
